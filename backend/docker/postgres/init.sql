-- PostgreSQL initialization script for CoachFlow
-- Note: The main database is created automatically via POSTGRES_DB environment variable
-- This file can be used for additional setup like extensions, roles, etc.

-- Example: Enable UUID extension (already enabled by default in PostgreSQL 13+)
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";